{
	"name": "@sandcaster/cloudflare-worker",
	"version": "0.0.0",
	"private": true,
	"type": "module",
	"scripts": {
		"dev": "wrangler dev",
		"deploy": "wrangler deploy",
		"test": "vitest run"
	},
	"dependencies": {
		"@cloudflare/sandbox": "^0.7.18",
		"@cloudflare/workers-types": "^4",
		"hono": "^4"
	},
	"devDependencies": {
		"vitest": "^4",
		"wrangler": "^4",
		"@sandcaster/ts-config": "workspace:*"
	}
}
